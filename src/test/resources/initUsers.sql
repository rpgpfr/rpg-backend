DROP TABLE IF EXISTS USERS;

CREATE TABLE USERS
(
    USERNAME      TEXT UNIQUE NOT NULL,
    EMAIL         TEXT UNIQUE NOT NULL,
    FIRST_NAME    TEXT DEFAULT NULL,
    LAST_NAME     TEXT DEFAULT NULL,
    PASSWORD      TEXT DEFAULT NULL,
    DESCRIPTION   TEXT DEFAULT NULL,
    RPG_KNOWLEDGE TEXT DEFAULT NULL,
    SIGNED_UP_AT  DATE DEFAULT '2025-01-01',
    PRIMARY KEY (USERNAME, EMAIL)
);

INSERT INTO USERS (USERNAME, EMAIL, FIRST_NAME, LAST_NAME, PASSWORD, SIGNED_UP_AT)
VALUES ('alvin', 'mail@example.com', 'Alvin', 'Hamaide', 'password', '2025-01-01');